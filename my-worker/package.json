{
  "name": "admin-panel",
  "version": "1.0.0",
  "description": "Cloudflare Worker with D1 and Admin Panel",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
